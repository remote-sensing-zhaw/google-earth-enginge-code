
// this is code is used to import the data
var srtm = ee.Image("USGS/SRTMGL1_003")

// to get metadata of the data (similar to R str() function)
console.log(srtm);

// in the GEE environment, print is the same as console.log 
// this is not the case outside GEE
print(srtm);


// add the data to the map
Map.addLayer(srtm)

// not much visible. Why not? the color white to black is streched 
// across the whole range of values. Visp, however steep it is there,
// does not cover this whole range. Let's truncate using the visParams
// In th docs for Map.addLayer() see the reference:
// For Images and ImageCollection, see ee.data.getMapId for valid parameters


Map.addLayer(srtm, {min: 0, max: 4000})


// add a color palette
Map.addLayer(srtm, {min: 0, max: 4000, palette: ["green", "red", "blue"]})